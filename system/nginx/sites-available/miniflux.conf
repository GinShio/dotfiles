{%@@ from 'nginx/macros.j2' import server_block_base, proxy_pass with context @@%}

{%@@ call server_block_base(vps.apps.rss.domain, vps.domain) @@%}
{{@@ proxy_pass(vps.apps.rss.bind, vps.apps.rss.port) @@}}
{%@@ endcall @@%}
