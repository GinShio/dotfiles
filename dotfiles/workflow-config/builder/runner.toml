[project]
org = "{{@@ projects.source_info['runner'].org @@}}"
name = "{{@@ projects.source_info['runner'].name @@}}"
source_dir = "{{@@ projects.root_dir @@}}/{{project.org}}/{{project.name}}"
build_dir = "_build/{{user.branch}}"
build_system = "cargo"
toolchain = "rustc"

[git]
url = "https://gitlab.freedesktop.org/mesa/deqp-runner.git"
main_branch = "main"
auto_stash = true
